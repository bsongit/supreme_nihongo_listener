import * as React from 'react';
import { Platform, View, Pressable, Text, Image, TextInput } from 'react-native';
import { IconButton, MD3Colors } from 'react-native-paper';
import Voice from '@react-native-voice/voice';

export default class QuestionModeScreen extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    componentDidMount() {
    }


    render() {
        return <View style={styles.container}>
                  <View style={styles.panel}>
                      <Text>PERGUNTA</Text>
                      <Text>RESPOSTA</Text>

                  </View>
                  <View style={styles.playerContairnerControl}>
                        <IconButton
                            icon="microphone"
                            mode= "contained"
                            iconColor={MD3Colors.error50}
                            size={32}
                            onPress={() => {}}
                        />
                    </View>
                </View>        
    }
}

const styles = {
    container: {
        backgroundColor: '#1f1e1e', 
        flex: 1, 
        width: '100%', 
        display: 'flex', 
        flexDirection: 'column',
        padding: 12
    },
    panel: {
      width: '100%',  
      height: 500,
      display: 'flex', 
      flexDirection: 'column', 
      alignItems: 'center', 
      justifyContent: 'center',
      borderWidth: 1, 
      borderRadius: 2, 
      borderColor: '#acb8bd'
    },
    playerContairnerControl: {
      with: '100%',
      borderWidth: 1, 
      borderRadius: 2, 
      borderColor: '#acb8bd',
      display: 'flex',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center'

  },
};